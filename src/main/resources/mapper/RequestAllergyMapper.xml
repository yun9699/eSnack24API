<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.esnack24api.esnack24api.community.mapper.RequestAllergyMapper">

    <select id="getRequestAllergyList" resultType="RequestAllergyDTO">
        select
            cano, caallergy, catitle, caanswer, cadelete, caregdate, camoddate
        from
            tbl_community_allergy
        where
            cadelete = 0
        order by
            cano desc
        limit #{pageRequest.skip}, #{pageRequest.size}
    </select>

    <select id="count" resultType="int">

        select count(cano)
        from tbl_community_allergy
        where cano > 0 and cadelete = false

    </select>

    <select id="getRequestAllergyDetail" resultType="RequestAllergyDTO">

        select
            cano, caallergy, catitle, caanswer, cadelete, caregdate, camoddate
        from
            tbl_community_allergy
        where
            cano = #{cano}
        and
            cadelete = false

    </select>

</mapper>